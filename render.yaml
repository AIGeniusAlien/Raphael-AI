
services:
  - type: web
    name: raphael-ai
    env: python
    plan: free
    buildCommand: ""
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: "10000"
healthCheckPath: /healthz
